## Setup skill maintenance

Whenever a new feature is added to this plugin that requires an external tool (LSP server, CLI tool, runtime, etc.), **always update `skills/setup/SKILL.md`** to include:

- The new tool in the required tools table
- The check command in Step 2
- The install command for each supported OS (macOS/Homebrew, Linux/apt) in Step 4
